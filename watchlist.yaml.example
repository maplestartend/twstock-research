# 自選股清單範本 —— 複製成 watchlist.yaml 後自行增刪（watchlist.yaml 已 gitignore，不進 repo）。
# 也可留空（或不建立此檔），啟動後直接用網頁「自選股管理」頁加入，程式會自動寫回 watchlist.yaml。
#
# 結構（v2，向後相容 v1）：
#   stocks: 代號 → 名稱
#   tags:   可選；代號 → 標籤陣列（沒有此鍵也能正常 load）
stocks:
  '2330': 台積電
  '0050': 元大台灣50
tags:
  '2330': [長期持有]
  '0050': [核心配置, 配息]
