__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
config.yaml
# 個人自選股清單（含個人關注標的）；範本走 watchlist.yaml.example
watchlist.yaml
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/cache/
data/backup/
.vscode/
.idea/
.pytest_cache/
*.log
logs/
reports/

# 個人 memory（跨 session 私人筆記，不進 repo）
memory/

# Web (Next.js)
web/node_modules/
web/.next/
web/out/
web/.turbo/

# Screenshot 輸出（每次跑會重產，不進 git；script 本身有進）
web/scripts/screenshots/
web/scripts/screenshots-all/
web/scripts/screenshots-mobile/
web/screenshots/
scripts/screenshots-all/

# 暫時除錯產物
probe*.json
probe*.html

# 私有 / 一次性分析 script（_ 前綴慣例，與 .bat 的 _launch-servers / _kill-servers 對齊）
# 涵蓋 .py / .csv / .txt / .json — agent 跑出的中間檔同樣不該進 repo
scripts/_*

# Claude Code 內部 schedule 狀態（lock 檔每次跑會推進，不進 git）
.claude/scheduled_tasks.lock
